const initHome = () => {
  // const title = gsap.timeline({defaults: {ease: 'power1.out'}})
  // title.to('.js-anime-text', {y: '0%', duration: 1, stagger: 0.25})
  
  // scrollElements = document.querySelector('.js-scroll')
}


window.addEventListener('load', initHome)